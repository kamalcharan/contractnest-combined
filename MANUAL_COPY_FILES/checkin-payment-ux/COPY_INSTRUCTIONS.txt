===============================================================
MANUAL COPY INSTRUCTIONS - checkin-payment-ux
===============================================================

IMPORTANT: The database migration in this batch is ALREADY LIVE
on Supabase (applied directly this session). Copy it so your
local checkout's git history matches what's running -- no
redeploy needed for that file.

FILES IN THIS FOLDER:
---------------------------------------------------------------

1. contractnest-edge/supabase/migrations/bbb-foundation/047_checkin_totals_and_cadence.sql
   -> Copy to: contractnest-edge\supabase\migrations\bbb-foundation\047_checkin_totals_and_cadence.sql
   -> ALREADY LIVE. gs_member_history now also returns:
        - totals: { total_amount, total_paid, balance } -- same numbers
          the seller's Financials tab already shows for this contract,
          just exposed to the public check-in RPC.
        - Each billing row: amount_settled + remaining (amount minus
          amount_settled) -- lets the UI cap what a member can enter.
        - cadence_rates -- the member's own real cadencePricing rate
          card (Monthly/Quarterly/etc with real amounts).

2. contractnest-ui/src/pages/checkin/useSessionCheckin.ts
   -> Copy to: contractnest-ui\src\pages\checkin\useSessionCheckin.ts
   -> New types: CheckinTotals, CadenceRate, CadenceRateCard.
      BillingRow gains amount_settled/remaining. CheckinHistory gains
      totals/cadence_rates.

3. contractnest-ui/src/pages/checkin/SessionCheckinPage.tsx
   -> Copy to: contractnest-ui\src\pages\checkin\SessionCheckinPage.tsx
   -> Redesigned "Your dues" -> "Payments" card, built entirely from the
      real billing events (no abstract "pick Monthly or Quarterly"
      picker -- the contract is already on one cadence; these ARE its
      events):
        - Total Paid / Balance up top (two stat tiles, same numbers as
          the Financials tab)
        - Payment History -- every already-paid event with its real
          label/date/amount, collapsed by default (tap to expand),
          same pattern as the seller's receipts list
        - A clear visual break, then CURRENT: the next open event's
          real label, due date, and exact amount -- no picker, no
          free-typed amount, because there's no real choice here, it's
          exactly what that event was priced at
        - UPI pay button / QR / reference field: unchanged mechanics,
          driven by the current due's amount
        - Submit button + the post-submit confirmation screen both
          show the actual amount being recorded

SUBMODULES AFFECTED: contractnest-ui, contractnest-edge
(edge already deployed live -- only ui needs your local test + merge)

TESTING CHECKLIST:
- [ ] Open a check-in link for a member with an open due, scan/identify
- [ ] "Total Paid" / "Balance" show correct numbers matching Financials tab
- [ ] "Payment History" expands to show real paid events with dates/amounts
- [ ] "CURRENT" shows the next real due event's label, date, and exact
      amount -- no free-text field, no picker, anywhere
- [ ] "Pay X now" / QR reflect that exact current-due amount
- [ ] Submit -> declaration recorded with that amount; confirmation screen
      shows it
- [ ] Member with zero open dues -> "All dues are settled" message
- [ ] Member with zero paid events -> no "Payment History" toggle shown
- [ ] Existing UPI pay-now / QR / reference flow still works end-to-end

===============================================================
