═══════════════════════════════════════════════════
BATCH: ops-timeboard (2026-09-18) — the TIMEBOARD: every hour, every person,
       every commitment; drag a service onto a time (or a person) and confirm
═══════════════════════════════════════════════════
POA batch 3 (owner: "start batch 3"). UI only — no migration, no API change.
The Plan tab (023) lines the days up; this puts the same cards on a clock,
reading the same two readers: jtd_plan for the rows and get_team_availability
(024) for the hours, days off, leave and holidays that hatch the columns.

Submodules affected:
  contractnest-ui — pages/ops/timeboard/index.tsx (NEW, the page),
                    components/ops/timeboard/model.ts (NEW — placement,
                    availability, clash preview, find-a-slot; pure functions),
                    components/ops/timeboard/Grid.tsx (NEW — Week + Day),
                    components/ops/timeboard/Agenda.tsx (NEW — phone default),
                    App.tsx (route /ops/timeboard; built on pristine main —
                    nothing unmerged touches App.tsx),
                    utils/constants/industryMenus.ts (menu entry "Timeboard"
                    after Commitments Register — a SUPERSET of batch
                    ops-menu-flatten's copy; copy this batch after it)
  ⚠ Depends on the unmerged batches ops-expense-board, ops-plan-view and
    ops-availability being copied in (useOpsPlan, useTeamAvailability,
    JobCard with clash pills, the Plan hooks). Copy order: expense-board →
    plan-view → availability → menu-flatten → THIS.

─── WHAT IT DOES (rebuilt 2026-09-18 to the PLAYGROUND shell after the owner:
    "you gave me a beautiful UX and what you built is entirely different") ───
 /ops/timeboard (menu: Timeboard; revenue side). The playground made real:
 TOP BAR  ‹ Today › · range · date · Day / Week / Agenda · View as (Everyone ·
          each teammate) · search · [Find a slot] · refresh.
 STRIP    N need you · N clashes · N to place · N awaiting customer · N
          unassigned · carried over → · parked → · VaNi on/off.
 RAIL     "Who": Everyone + each person (avatar, load bar = timed minutes /
          working minutes over the visible days, hours line or "day off",
          clashes) — click = View as. "VaNi" card with the autonomy dial
          Draft / Ask me / Auto (client preference, localStorage).
 BOARD    "To place" tray = every unslotted service in the window + carried,
          GROUPED by contract + block and collapsed by default (fix after the
          first screenshot, 2026-09-18: 27 chips had pushed the grid below the
          fold) — header "27 to place · 22 overdue · 4 contracts", one chip per
          group with ×N and the oldest due, click a group to open its services
          (draggable, one by one), "Show all" opens every group; the tray is
          height-capped and scrolls, so the grid always stays above the fold;
          then the grid — DAY = a column per person + Unassigned + VaNi,
          WEEK = a column per day, AGENDA = the list. Hatched off-hours /
          day off / holiday / leave, now rule, blocks sized by duration,
          side by side on overlap, ⚠ on a clash; a person's follow-ups at
          their due time and the ladder's reminders in VaNi's column (Day).
 SIDE     (400px; the card renders compact so the evidence line does not wrap
          word by word) The selected item = the board's own JobCard with every verb, the
          CONFIRM BAR under it after a drop ("Move to Tue 22 Sep 2 pm · Ravi?"
          → Move / Move & confirm / Keep as is; a confirmed slot reads "This
          slot was confirmed with the customer. Move … and ask them again?"
          → Move & ask / Move, keep confirmed), the clash preview, and Find a
          slot. Otherwise "VaNi suggests" (from the counts: place N, ask M,
          reminders, clashes — buttons on Ask me / Auto, text only on Draft;
          greyed with Open VaNi when VaNi is off) and "Your day" (the signed-
          in user's rows today). A list (carried / parked / a roll-up chip)
          opens here too.
 BACKLOG  "VaNi suggests" leads with the carried-over services: "22 services
          are overdue since 15 Aug with no slot — about 3 working days of work.
          Spread them over the coming days rather than today" with "Find a slot
          for the oldest" (manual, never gated). Spreading them automatically is
          POA batch 4.
 GESTURE  drag a service (tray or grid) onto a time — Day view: onto a person
          (assigns first) → confirm bar → jtd_schedule_visit (proposed, or
          confirmed) → toast with UNDO (puts the previous slot back through
          the same tool; a first placement has no undo — nothing un-slots).
 FIND A SLOT  modal: What (the tray) · Who (any / a technician) · From →
          next free times over 14 days inside working hours, skipping days
          off, leave, holidays and timed services → pick → confirm bar.
 Only services drag. Follow-ups: no item-reschedule API route yet.

 Verified: UI tsc 1170 = pristine on the assembled state (expense-board +
 items-on-jtd + plan-view + availability + menu-flatten + this); vite build
 passes. The grids were rendered in a throwaway harness with mocked rows
 (week / day / agenda screenshots checked); the rebuilt shell was NOT
 rendered (no auth from here) — first local run is the test.

═══════════════════════════════════════════════════
💻 PHASE 1: COPY FILES FOR LOCAL TESTING
═══════════════════════════════════════════════════
cd "D:\projects\core projects\ContractNest\contractnest-combined"
git pull origin claude/wonderful-knuth-kb61tu
# (if not already copied) the batches this one builds on, in this order:
Copy-Item "MANUAL_COPY_FILES\ops-expense-board\contractnest-ui\*" -Destination "contractnest-ui\" -Recurse -Force
Copy-Item "MANUAL_COPY_FILES\ops-plan-view\contractnest-ui\*"     -Destination "contractnest-ui\" -Recurse -Force
Copy-Item "MANUAL_COPY_FILES\ops-availability\contractnest-ui\*"  -Destination "contractnest-ui\" -Recurse -Force
Copy-Item "MANUAL_COPY_FILES\ops-menu-flatten\contractnest-ui\*"  -Destination "contractnest-ui\" -Recurse -Force
# this batch
Copy-Item "MANUAL_COPY_FILES\ops-timeboard\contractnest-ui\*"     -Destination "contractnest-ui\" -Recurse -Force
Write-Host "✅ ops-timeboard copied" -ForegroundColor Green
cd contractnest-ui; npm run dev      # Ctrl+F5

═══════════════════════════════════════════════════
🧪 TESTING CHECKLIST — signia, Live
═══════════════════════════════════════════════════
- [ ] Menu shows "Timeboard"; /ops/timeboard opens the Day view: rail (Who +
      VaNi dial), To place tray, grid (people + Unassigned + VaNi), side
      panel with "VaNi suggests" + "Your day".
- [ ] Column headers read the hours saved on /settings/configure/cadence;
      Sunday reads "day off" and is hatched; a marked holiday shows its label.
- [ ] Services with a slot appear as blocks at their time; unslotted ones as
      dashed chips in the top strip; payments as "N due" chips that open a list.
- [ ] Drag a chip onto Tue 11:00 → the confirm bar names the service, the
      day and time → Propose to customer → toast "Slot proposed for …" → the
      block appears at 11:00 (dashed amber); the same card on Ops reads "Slot
      proposed".
- [ ] Drag a block onto 08:00 → the bar shows "⚠ outside 09:00–18:00" before
      you confirm; Confirm slot → the toast repeats the warning, the block
      carries ⚠.
- [ ] Day view: columns per person + Unassigned + VaNi; drag a service from
      Unassigned onto a person at 14:00 → bar reads "… and assign to <name>"
      → Propose → the card is assigned and slotted; a second service on the
      same person at 14:30 → "overlaps …" in the bar.
- [ ] Tap a block → side panel with the full card; Find a slot → next free
      times listed; pick one → confirm bar → Propose.
- [ ] Day view VaNi line: "Plan this day · N" places the unslotted services;
      the VaNi column shows the ladder's reminders at 10:00.
- [ ] Agenda on a phone width: days listed, rows in time order, tap opens
      the panel; nothing draggable there (Schedule from the card works).
- [ ] buyer@b.com (VaNi off): reminders greyed, "Open VaNi"; expense
      perspective shows the one-line note.
═══════════════════════════════════════════════════
⏸️ WAITING FOR CONFIRMATION
  → "Tested, working - proceed with merge"
  → "Issue found: [describe problem]"
═══════════════════════════════════════════════════
