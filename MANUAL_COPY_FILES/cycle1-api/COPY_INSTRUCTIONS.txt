============================================================================
SmartForms — Cycle 1 Step 3: API Layer (Express Proxy to Edge)
============================================================================

SUBMODULES AFFECTED: contractnest-api

FILES:
  1. contractnest-api/src/types/adminForms.dto.ts
     → Request/Response DTOs for all Smart Forms endpoints

  2. contractnest-api/src/validators/adminFormsValidators.ts
     → express-validator chains for all routes (matches adminJtdValidators pattern)

  3. contractnest-api/src/services/adminFormsService.ts
     → Edge function proxy — axios calls to smart-forms edge function

  4. contractnest-api/src/routes/adminFormsRoutes.ts
     → Express router with 12 endpoints (matches adminJtdRoutes pattern)

  5. contractnest-api/src/index.ts
     → Updated with route registration at /api/admin/forms (included in copy)

API ROUTES:
  GET    /api/admin/forms              — List templates (filters + pagination)
  GET    /api/admin/forms/:id          — Get single template
  POST   /api/admin/forms              — Create template (admin)
  PUT    /api/admin/forms/:id          — Update draft (admin)
  DELETE /api/admin/forms/:id          — Delete draft (admin)
  POST   /api/admin/forms/validate     — Validate schema JSON (no DB)
  POST   /api/admin/forms/:id/clone          — Clone to new draft
  POST   /api/admin/forms/:id/submit-review  — draft → in_review
  POST   /api/admin/forms/:id/approve        — in_review → approved
  POST   /api/admin/forms/:id/reject         — in_review → draft (notes required)
  POST   /api/admin/forms/:id/new-version    — approved → past + new draft
  POST   /api/admin/forms/:id/archive        — approved → past

CHECKLIST:
  ✅ express-validator (NOT Zod) — matches existing pattern
  ✅ DTOs for all request/response types
  ✅ Service class as singleton (matches AdminJtdService)
  ✅ No business logic in API layer — pure proxy to edge
  ✅ Standard error handling via handleEdgeError + generateRequestId
  ✅ Proper HTTP status codes (201 for create/clone/new-version)
  ✅ index.ts route registration included

============================================================================
