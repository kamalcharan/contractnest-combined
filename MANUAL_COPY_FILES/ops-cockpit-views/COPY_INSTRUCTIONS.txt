═══════════════════════════════════════════════════
BATCH: ops-cockpit-views (2026-09-17) — Ops cockpit step 1 of the review:
       Coming up gets a List / Lanes switch and a 30 / 60 / 90 day horizon
═══════════════════════════════════════════════════
Owner decisions (2026-09-17): the switch applies to COMING UP only — Needs
you stays an ordered decision list. Steps 2+3 (paging + filters) follow as
one batch on the same reader; 4 VaNi chip; 5 Follow up; 6 contract audit
timeline with JTD rows.

REQUIRES ops-collections-tools to be copied first (hook, API, route). This
batch changes ONE file, and the same file has also been updated inside
ops-collections-tools, so copying either batch last gives the same page.

Submodules affected: contractnest-ui ONLY. No API/DB change.

─── FILE ────────────────────────────────────────────
 contractnest-ui/src/pages/ops/cockpit/Commitments.tsx
   Coming up header gains two segmented controls:
     · Horizon  30 d · 60 d · 90 d  → passed to the worklist reader
       (p_horizon_days; the API clamps 1–120). The sentence and the section
       note follow ("… falls due in the next 60 days").
     · View     List · Lanes
       List  = as before, payments and next rungs interleaved by when.
       Lanes = four columns by when: today · next 3 days · next 2 weeks ·
               rest of the month (or "15–60 days" / "15–90 days"). Payment
               cards (rupee mark, solid border) and rung cards (channel icon,
               dashed border) share a column; each column header counts
               "N pay · M rung". Horizontal scroll under 4×230px.
   Both views render ONE row model (payment | rung, days-until), so paging
   later applies per column or per section without a rewrite. Search applies
   to both. Choices persist per browser (localStorage, guarded — private
   windows fall back to List / 30 d).

 Verified: UI tsc 1170 = pristine main @ a2e8576 (0 in touched file); vite
 build passes.

─── POWERSHELL COPY (from contractnest-combined root) ───

Copy-Item "MANUAL_COPY_FILES\ops-cockpit-views\contractnest-ui\*" -Destination "contractnest-ui\" -Recurse -Force

─── TESTING CHECKLIST (BBB) ─────────────────────────
 [ ] Coming up shows "30 d · 60 d · 90 d" and "List · Lanes" on the right of
     the section head. Default List / 30 d.
 [ ] Lanes: "next 2 weeks" holds the 28 Sep quarterly instalments (payment
     cards) and the day-3 / day-7 rungs of recently nudged jobs (dashed rung
     cards); "today" reads "—" unless something lands today.
 [ ] 90 d: the sentence changes to "… in the next 90 days", the count grows,
     the "15–90 days" column fills; list view shows the same rows in order.
 [ ] Search "CN-1024" narrows both views to that contract.
 [ ] Reload: the chosen view and horizon are remembered.
 [ ] Needs you is unchanged (still the list with actions).
