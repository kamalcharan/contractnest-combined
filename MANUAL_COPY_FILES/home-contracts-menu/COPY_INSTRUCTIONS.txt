═══════════════════════════════════════════════════
BATCH: home-contracts-menu (2026-09-16)
═══════════════════════════════════════════════════
Owner requests:
 1. Hide classic Contracts menu (routes stay registered)
 2. NContracts renamed to "Contracts" (path stays /ncontracts)
 3. Claim Contract promoted to main menu
 4. Workspace Account card on /experience shows numbers:
    contracts used / open vs plan cap + WhatsApp credits available
    + "Top up" link to the subscription page (existing pack-purchase flow)
    (WhatsApp CONSUMED count deliberately deferred — needs get_tenant_context
    change; to be discussed)
 5. "Speak to an expert" support card on /experience
    WhatsApp wa.me/919949701175 · support@vikuna.io

Submodules affected: contractnest-ui ONLY. No API, edge, DB or mobile changes.

FILES (copy to the same relative path inside contractnest-ui):

 1. src/utils/constants/industryMenus.ts
    - ncontracts entry relabelled "Contracts" (id unchanged)
    - top-level Claim Contract entry added
    - classic contracts group commented out (un-hide by uncommenting)

 2. src/pages/experience/index.tsx
    - "Open contracts" (empty state) and "View all" links now /ncontracts
    - SupportCard added to the right-hand column

 3. src/pages/experience/TenantAccountNotice.tsx
    - numbers strip: contracts used/open vs cap, WhatsApp credits, Top up link
    - all values from the existing useTenantContext payload; rows omitted
      (not zeroed) when a field is absent

 4. src/pages/experience/SupportCard.tsx  (NEW)
    - support contacts are two constants at the top of the file

 5. src/pages/experience/experience.css
    - appended .xp-account-stats and .xp-support styles
    - appended the VIBRANCY PASS block (owner, 2026-09-16): heading gradient,
      per-action Start-card accents, colored status pills, tinted icon chips,
      shortcut dots, contracts-allowance meter. All colors are theme tokens
      (brand/success/warning/info/error) via color-mix — dark mode + all
      tenant themes stay correct automatically.

 6. src/pages/experience/StartChooser.tsx
    - each action card gets an xp-choice-<action> class for its accent
      (create=brand, record=green, request=amber, respond=blue, claim=blend)

 7. src/pages/experience/index.tsx (same file as #2, more changes)
    - exposes --xp-warning/--xp-info/--xp-error from the theme
    - status pills colored by meaning (active=green, draft=amber,
      pending/sent=blue, expired/cancelled=red, unknown=neutral)
    - foundation links get tinted icon chips

 8. src/pages/experience/TenantAccountNotice.tsx (same file as #3)
    - contracts allowance meter bar (green -> amber near limit -> red over,
      driven by the existing near_limit/over_limit flags; advisory only)
    - WhatsApp credits value turns amber when credits_low

POWERSHELL COPY (from contractnest-combined root):

Copy-Item "MANUAL_COPY_FILES\home-contracts-menu\contractnest-ui\*" -Destination "contractnest-ui\" -Recurse -Force

TESTING CHECKLIST:
 [ ] Sidebar: no classic Contracts group; "Contracts" (→ /ncontracts) and
     "Claim Contract" visible in both Revenue and Expense perspectives
 [ ] /contracts and /contracts/:id still open via direct URL / row clicks
 [ ] /experience: account card shows "Contracts: N used · M open of X"
     and "WhatsApp credits: N available"; Top up opens subscription page
 [ ] Numbers match /businessmodel/tenants/subscription for the same tenant
 [ ] Support card: WhatsApp opens wa.me/919949701175 in new tab with preset
     text; email opens mail client to support@vikuna.io
 [ ] Home empty-state "Open contracts" and list "View all" land on /ncontracts
 [ ] Lite tenant (CNAK/RFQ-lite) Home unchanged (LiteDashboard, no crash)
 [ ] VIBRANCY: heading band shows soft brand->blue gradient; Start cards
     each have their own accent (top border + icon chip); status pills
     colored by state; allowance meter renders under Contracts numbers
 [ ] Toggle dark mode and at least one other tenant theme — all accents
     recolor with the theme, text stays readable everywhere

═══════════════════════════════════════════════════
ADDENDUM (2026-09-16): theme fixes in the NEW contracts UX
═══════════════════════════════════════════════════
Four more files (same copy command covers them):

  9. src/components/contracts/ContractWizard/experience/creation.css
     - primary footer button text now var(--cc-brand-text) (was #fff);
       readable on light-brand tenant themes
 10. src/components/contracts/ContractWizard/experience/review.css
     - [role=alert] error text now var(--fr-error) (was #bd2626);
       readable in dark mode
 11. src/components/contracts/ContractWizard/experience/ReviewPage.tsx
     - maps --fr-error from the theme's semantic.error
 12. src/components/contracts/ContractWizard/experience/CommitmentEditor.tsx
     - discount green + cadence final-payment amber block now use
       semantic.success / semantic.warning (were #0d9464, #B45309, #F59E0B60)

NOT changed (deliberate): the Review page's contract document keeps its
forced white-paper look (it is the printable document); the Client/Partner/
Vendor badge colors in agreement.css stay as fixed identity colors pending
an owner call.

Extra test items:
 [ ] Experience wizard footer "Continue/Create" button text readable on the
     current theme (and any light-brand theme if available)
 [ ] Review chapter: trigger the acceptance alert in dark mode - error text
     clearly visible
 [ ] Commitment editor: cadence with leftover months shows the amber
     final-payment block in the theme's warning color (check dark mode)
