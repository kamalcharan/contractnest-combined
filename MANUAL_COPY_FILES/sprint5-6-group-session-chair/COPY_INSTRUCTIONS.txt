═══════════════════════════════════════════════════
SPRINT 5+6 (COMBINED) — GROUP SESSION CHAIR ASSIGNMENT (corrected)
Branch folder: sprint5-6-group-session-chair
Submodules affected: contractnest-edge ONLY (DB/RPC — already applied live)
Supersedes: sprint5-6-group-session-occurrence (REMOVED — see below)
═══════════════════════════════════════════════════

CORRECTION, READ THIS FIRST
────────────────────────────────────────────────────
The previous batch (sprint5-6-group-session-occurrence) built a brand-new
table (t_session_occurrence_assignments) + 2 RPCs to solve "assign one
instructor per Group Session occurrence, fan out to every member" —
reasoning from t_contract_events/t_appointments alone, WITHOUT first
searching the codebase for an existing Group Sessions backend. The owner
then showed a live Group Sessions dashboard (localhost:5173/group-
sessions, Test environment) that the earlier live-only DB queries had
completely missed.

Ground truth: Group Sessions already have a full, mature backend
(bbb-foundation/021 onward, ~26 gs_* RPCs) — occurrences already live in
t_group_session_schedule (ONE row per tenant+block+date, generated by
gs_generate_schedule() from the catalog block's cadence config,
decoupled from t_contract_events entirely — member contracts carry the
block via t_contract_blocks, they do NOT spawn one event per member).
Attendance, roster, QR check-in (self/guest/substitute), dues-paid
marking, and a dashboard all already exist and are live.

The earlier batch was REDUNDANT with this — a parallel occurrence
concept solving an already-solved problem. It has been REVERTED live
(0 rows ever existed, 0 dependents, confirmed before dropping) and its
MANUAL_COPY_FILES folder deleted. This batch replaces it.

WHAT THIS BATCH ACTUALLY DOES
────────────────────────────────────────────────────
The ONE real gap in the existing system: t_group_session_schedule had no
column for who chairs/hosts a given occurrence — exactly what the owner
asked for ("a Group Session is headed by someone... he will have an
appointment against 'Group Session' in the cadence").

- ALTER t_group_session_schedule: adds assigned_to (uuid),
  assigned_to_name (text). No new table.
- gs_dash_occurrences() (existing RPC): additive change, now also
  returns assigned_to/assigned_to_name per occurrence. Everything else
  reproduced verbatim from the live definition.
- New RPC gs_schedule_assign(tenant, occurrence_id, assigned_to,
  assigned_to_name) — sets who chairs ONE occurrence (referenced by its
  own schedule-row id, since occurrences already have one), returns the
  refreshed occurrence list via gs_dash_occurrences(). Matches the
  existing gs_schedule_status/gs_schedule_move convention exactly (same
  {ok:false, reason:'not_found'} error shape, no exception wrapper —
  this RPC family doesn't use one).

This is a ~15-line fix, not a new subsystem — because the subsystem
already existed.

VERIFICATION (already done, live, before this file was written)
────────────────────────────────────────────────────
- Real BBB occurrence, Saturday Cadence, 2026-07-31 (id
  0f880207-7d68-41c0-8934-d2a4a89e29f2): called gs_schedule_assign with
  a random uuid + "Test Chair (verify only)" — the refreshed 27-
  occurrence list showed assigned_to set on ONLY that one row (seq 2),
  all 26 other occurrences (past and future) untouched, matching exactly
  the fan-out-free, one-occurrence-at-a-time design intended.
- Test assignment then cleared back to NULL/NULL on that same row — no
  fake data left in BBB's real schedule.
- Confirms BBB actually has 27 active member contracts carrying
  "Saturday Cadence" (not the ~10 sampled earlier — LIMIT 10 undercounted).

FOLLOW-UP (031_group_session_chair_appointment.sql, same session)
────────────────────────────────────────────────────
Owner's next requirement: assigning a chair should ALSO create a real
appointment for that person, not just a field on the occurrence. This is
architecturally sound (unlike the reverted first attempt) because it's
genuinely 1:1 — one occurrence, one chair, one appointment — exactly
t_appointments' intended cardinality, unlike "fan one appointment across
50 members" which was correctly ruled out.

- t_appointments widened: contract_id/event_id made nullable, new
  nullable group_session_occurrence_id (FK to t_group_session_schedule),
  a CHECK constraint enforcing exactly one link type is ever set, and a
  partial unique index (one active appointment per occurrence, mirroring
  the existing per-event rule).
- gs_schedule_assign() extended: on assigning, also upserts the chair's
  appointment — status='accepted' directly (no requested/negotiation
  stage; the date's already fixed by cadence, the tenant is assigning,
  not proposing slots for the chair to pick). Passing assigned_to=NULL
  now also soft-deletes the appointment (is_active=false), not just
  clearing the occurrence field.
- New RPC gs_schedule_assign_default(tenant, block, is_live,
  assigned_to, assigned_to_name) — "set as default chair going forward":
  applies to every future, non-cancelled occurrence of a block in one
  call, each getting its own appointment row. A later single-occurrence
  gs_schedule_assign() call still overrides just that one date (owner:
  "default to all, but might change at a session").

Also settled and declined in this session: merging Group Session
occurrences directly into t_contract_events (a "universal cadence
table" with a type discriminator). Technically feasible, but would mean
rewriting ~26 already-live gs_* RPCs + the working dashboard for a
conceptual-purity gain that doesn't even solve the attendance-table
question (t_session_attendance still can't fold into
t_contract_event_assets either way — attendance is per (occurrence,
member) with no per-member event to attach to, which is deliberately
avoided). Owner agreed: not worth the rewrite risk now; a read-layer
UNION view can give VaNi a unified feed later without touching the live
system.

VERIFICATION (031, already done, live, before this file was written)
────────────────────────────────────────────────────
- gs_schedule_assign on the same real occurrence (Jul 31): appointment
  row created with contract_id=NULL, event_id=NULL,
  group_session_occurrence_id set, status='accepted', scheduled_at =
  occurrence date, is_live correctly inherited from the occurrence
  (false, matching Test-environment data).
- gs_schedule_assign(..., NULL, NULL) on the same occurrence:
  appointment flipped to is_active=false (soft-deleted), occurrence's
  assigned_to/assigned_to_name cleared.
- gs_schedule_assign_default on the full "Saturday Cadence" block:
  assigned_count=26 (all future, non-cancelled occurrences — the one
  past occurrence correctly excluded), confirmed 26 active appointment
  rows created, one per occurrence.
- Full cleanup after verification: all test appointment rows hard-
  deleted, all occurrences' assigned_to/assigned_to_name cleared back to
  NULL — confirmed zero residue (remaining_assigned=0,
  remaining appointments query returned no rows) before writing this
  file.

NOT YET DONE (next phases)
────────────────────────────────────────────────────
- UI: "Assign chair" action (single + "set as default") on the Group
  Sessions dashboard (localhost:5173/group-sessions), using the existing
  common/ContactPicker.tsx (classifications: buyer/vendor/partner/
  team_member) for who to pick, plus a small "Appointment Card" showing
  the resulting appointment (Accepted status, editable) inline — not
  built in this batch, pure backend so far.
- API layer: POST /api/group-sessions/occurrence/:id/assign (+ a
  "default" variant) — route/controller/service + serviceURLs.ts entry +
  a mutation hook in useGroupSessionsDashboard.ts. Not built yet.
- Sprint 5(b) data repair (stale event statuses, orphaned test events,
  the 29 stuck 'requested' appointments — platform-wide, unrelated to
  Group Sessions).
- Sprint 6 1:1 appointment kanban accept-flow work — needs a tenant with
  real appointment data; BBB has zero.

Nothing UI-facing in this batch — no copy/test steps for contractnest-ui.
═══════════════════════════════════════════════════
