═══════════════════════════════════════════════════
COPY INSTRUCTIONS - Global Templates UX Fixes
═══════════════════════════════════════════════════
Branch: feat/global-templates-ux-fixes
Date: 2026-03-23

Submodules Affected: contractnest-ui

Files to Copy:
──────────────
1. contractnest-ui/src/pages/service-contracts/templates/admin/global-templates.tsx
   → Copy to: contractnest-ui/src/pages/service-contracts/templates/admin/global-templates.tsx
   → Purpose: Lucide icons, DB resource types, nomenclature horizontal pills

Changes Summary:
──────────────
1. ICONS: Replaced INDUSTRY_EMOJI_MAP emojis with dynamic Lucide icon rendering
   - Uses (LucideIcons as any)[iconName] pattern (same as IndustrySelector.tsx)
   - Falls back to Circle icon if name is null/invalid

2. RESOURCE TYPES: Replaced hardcoded RESOURCE_TYPES with useResourceTypes() hook
   - Fetches real data from /api/resources/resource-types
   - Shows loading skeleton while fetching
   - Only shows active resource types

3. NOMENCLATURE: Moved from sidebar accordion to HORIZONTAL COLORED PILLS
   - Sits in the search/controls bar area, between search row and active filters
   - Uses useNomenclatureTypes() hook (cat_contract_nomenclature master data)
   - All items shown flat as colored pills (no expand/collapse needed)
   - Grouped visually with subtle labels: Equipment: [AMC] [CAMC] [TEC] | Facility: [FMC] [CMC] | etc.
   - Each pill: colored dot (hexcolor from DB) + short_name (AMC, CMC)
   - Hover tooltip shows full_name + typical_duration
   - Click to select, click again to deselect (toggles back to "All")
   - Loading skeleton pills while data fetches
