═══════════════════════════════════════════════════
COPY INSTRUCTIONS - Global Templates UX Fixes
═══════════════════════════════════════════════════
Branch: feat/global-templates-ux-fixes
Date: 2026-03-23

Submodules Affected: contractnest-ui

Files to Copy:
──────────────
1. contractnest-ui/src/pages/service-contracts/templates/admin/global-templates.tsx
   → Copy to: contractnest-ui/src/pages/service-contracts/templates/admin/global-templates.tsx
   → Purpose: Replace emoji icons with Lucide, DB resource types, add nomenclature filter

Changes Summary:
──────────────
1. ICONS: Replaced INDUSTRY_EMOJI_MAP emojis with dynamic Lucide icon rendering
   - Uses (LucideIcons as any)[iconName] pattern (same as IndustrySelector.tsx)
   - Falls back to Circle icon if name is null/invalid

2. RESOURCE TYPES: Replaced hardcoded RESOURCE_TYPES with useResourceTypes() hook
   - Fetches real data from /api/resources/resource-types
   - Shows loading skeleton while fetching
   - Only shows active resource types

3. NOMENCLATURE: Added new filter section in sidebar
   - Uses useNomenclatureTypes() hook (cat_contract_nomenclature master data)
   - Grouped accordion: Equipment Maintenance, Facility Property, Service Delivery, Flexible Hybrid
   - Each item shows colored dot, short code (AMC, CMC), and full name
   - Collapsible groups via ChevronDown toggle
   - Filter chip in active filters bar

New Imports Added:
──────────────
- import * as LucideIcons from 'lucide-react'
- import { useResourceTypes, ResourceType as DBResourceType } from '@/hooks/queries/useResources'
- import { useNomenclatureTypes, NomenclatureGroup } from '@/hooks/queries/useNomenclatureTypes'
- ChevronDown, Circle, Package from lucide-react (named imports)

Removed Imports:
──────────────
- RESOURCE_TYPES from globalTemplateData
- resolveIndustryIcon from globalTemplateData
