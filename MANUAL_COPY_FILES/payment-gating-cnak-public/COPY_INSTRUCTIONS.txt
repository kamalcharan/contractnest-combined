═══════════════════════════════════════════════════
📦 CHANGES SUMMARY
═══════════════════════════════════════════════════
Branch: claude/init-submodules-2qx34t
Submodules Affected: contractnest-api, contractnest-ui, contractnest-edge (docs-only —
migrations already applied live via Supabase MCP; the .sql files here are
source-of-record copies for git, DO NOT re-run them against the DB)

Files Changed:
  contractnest-api/
    src/services/publicPaymentService.ts          (NEW) CNAK-scoped payment RPC wrapper
    src/services/integrationService.ts             QR image upload (Firebase Storage)
    src/controllers/contractController.ts           5 new public payment endpoints
    src/controllers/paymentGatewayController.ts     declaration list/confirm (authenticated)
    src/controllers/integrationController.ts        QR upload controller
    src/routes/contractRoutes.ts                    public payment routes
    src/routes/paymentGatewayRoutes.ts               declaration routes
    src/routes/integrationRoutes.ts                  QR upload route (multer)

  contractnest-ui/
    src/hooks/useIntegrations.ts                    ConfigField 'image' type
    src/components/integrations/DynamicFormField.tsx image upload UI (preview/replace/remove)
    src/services/serviceURLs.ts                     UPLOAD_QR + PUBLIC_PAYMENT_* endpoint constants
    src/components/contracts/review/PublicPaymentSection.tsx (NEW) payment step for /contract-review
    src/pages/contracts/review/index.tsx            payment section wiring + Accept button swap

  contractnest-edge/  (docs only — already live)
    supabase/migrations/business-model-v2/031_generalize_payment_gated_entitlements.sql
    supabase/migrations/business-model-v2/032_public_payment_collection.sql
    supabase/migrations/business-model-v2/033_fix_record_invoice_payment_acceptance_method.sql
    supabase/migrations/business-model-v2/034_offline_upi_qr_and_declaration_listing.sql
    supabase/migrations/business-model-v2/035_respond_to_contract_payment_gate.sql

Production Checklist:
  ✅ Transaction Management: reuses record_invoice_payment_with_allocations (existing,
     transactional); unique partial index blocks duplicate pending declarations
  ✅ Race Condition Handling: FOR UPDATE locks in the underlying RPCs (pre-existing,
     unchanged); ON CONFLICT on the declarations table
  ✅ Error Handling: every new endpoint validates inputs, maps RPC failures to HTTP codes
  ✅ Toasts: vaniToast (existing component) on QR upload success/failure
  ✅ Loaders: spinner on the QR upload button while uploading

NOW ALSO INCLUDED (added after the first cut of this batch):
  - /contract-review page payment step: Razorpay checkout button, offline-UPI
    VPA + QR + declare-reference form, and the "[Tenant] has been notified and
    will connect with you for further steps" fallback when neither is
    configured. The Accept button is replaced for payment-gated contracts.
  - respond_to_contract payment gate (migration 035, live): plain accept on an
    unpaid payment-gated contract is refused server-side (PAYMENT_REQUIRED).
  - Cadence picker is disabled for payment-gated contracts (invoice is
    generated from the proposed plan when the payment step loads; a later
    cadence switch would desync the invoice).

WHAT'S STILL NOT DONE (separate next steps):
  - Tenant-facing UI to review/confirm pending declarations (routes exist:
    GET /api/payments/declarations, POST /api/payments/declarations/:id/confirm).
  - Pricing-card visual redesign — separate, already-agreed item.

═══════════════════════════════════════════════════
💻 STEP-BY-STEP COMMANDS (Windows PowerShell)
═══════════════════════════════════════════════════

STEP 1: Pull the Feature Branch
─────────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"
git fetch origin
git checkout claude/init-submodules-2qx34t
git pull origin claude/init-submodules-2qx34t

STEP 2: Verify Files Received
─────────────────────────────────
ls MANUAL_COPY_FILES\payment-gating-cnak-public
type MANUAL_COPY_FILES\payment-gating-cnak-public\COPY_INSTRUCTIONS.txt

STEP 3: Copy All Files (PowerShell)
─────────────────────────────────
$src = "MANUAL_COPY_FILES\payment-gating-cnak-public"

Copy-Item "$src\contractnest-api\src\services\publicPaymentService.ts" -Destination "contractnest-api\src\services\publicPaymentService.ts" -Force
Copy-Item "$src\contractnest-api\src\services\integrationService.ts" -Destination "contractnest-api\src\services\integrationService.ts" -Force
Copy-Item "$src\contractnest-api\src\controllers\contractController.ts" -Destination "contractnest-api\src\controllers\contractController.ts" -Force
Copy-Item "$src\contractnest-api\src\controllers\paymentGatewayController.ts" -Destination "contractnest-api\src\controllers\paymentGatewayController.ts" -Force
Copy-Item "$src\contractnest-api\src\controllers\integrationController.ts" -Destination "contractnest-api\src\controllers\integrationController.ts" -Force
Copy-Item "$src\contractnest-api\src\routes\contractRoutes.ts" -Destination "contractnest-api\src\routes\contractRoutes.ts" -Force
Copy-Item "$src\contractnest-api\src\routes\paymentGatewayRoutes.ts" -Destination "contractnest-api\src\routes\paymentGatewayRoutes.ts" -Force
Copy-Item "$src\contractnest-api\src\routes\integrationRoutes.ts" -Destination "contractnest-api\src\routes\integrationRoutes.ts" -Force

Copy-Item "$src\contractnest-ui\src\hooks\useIntegrations.ts" -Destination "contractnest-ui\src\hooks\useIntegrations.ts" -Force
Copy-Item "$src\contractnest-ui\src\components\integrations\DynamicFormField.tsx" -Destination "contractnest-ui\src\components\integrations\DynamicFormField.tsx" -Force
Copy-Item "$src\contractnest-ui\src\services\serviceURLs.ts" -Destination "contractnest-ui\src\services\serviceURLs.ts" -Force
New-Item -ItemType Directory -Force -Path "contractnest-ui\src\components\contracts\review" | Out-Null
Copy-Item "$src\contractnest-ui\src\components\contracts\review\PublicPaymentSection.tsx" -Destination "contractnest-ui\src\components\contracts\review\PublicPaymentSection.tsx" -Force
Copy-Item "$src\contractnest-ui\src\pages\contracts\review\index.tsx" -Destination "contractnest-ui\src\pages\contracts\review\index.tsx" -Force

Copy-Item "$src\contractnest-edge\supabase\migrations\business-model-v2\031_generalize_payment_gated_entitlements.sql" -Destination "contractnest-edge\supabase\migrations\business-model-v2\031_generalize_payment_gated_entitlements.sql" -Force
Copy-Item "$src\contractnest-edge\supabase\migrations\business-model-v2\032_public_payment_collection.sql" -Destination "contractnest-edge\supabase\migrations\business-model-v2\032_public_payment_collection.sql" -Force
Copy-Item "$src\contractnest-edge\supabase\migrations\business-model-v2\033_fix_record_invoice_payment_acceptance_method.sql" -Destination "contractnest-edge\supabase\migrations\business-model-v2\033_fix_record_invoice_payment_acceptance_method.sql" -Force
Copy-Item "$src\contractnest-edge\supabase\migrations\business-model-v2\034_offline_upi_qr_and_declaration_listing.sql" -Destination "contractnest-edge\supabase\migrations\business-model-v2\034_offline_upi_qr_and_declaration_listing.sql" -Force
Copy-Item "$src\contractnest-edge\supabase\migrations\business-model-v2\035_respond_to_contract_payment_gate.sql" -Destination "contractnest-edge\supabase\migrations\business-model-v2\035_respond_to_contract_payment_gate.sql" -Force

Write-Host "✅ All files copied!" -ForegroundColor Green

═══════════════════════════════════════════════════
🧪 TESTING CHECKLIST — do this BEFORE Step 4 below
═══════════════════════════════════════════════════
cd contractnest-api && npm run dev
cd ..\contractnest-ui && npm run dev   (separate terminal)

  All click-testable on the /contract-review page now (open a payment-gated
  contract's CNAK link: /contract-review?cnak=...&secret=...):

  [ ] Settings → Integrations → Offline UPI card → upload a QR image (PNG/JPG)
      → preview shows, "Replace"/remove work, persists after page reload
  [ ] FALLBACK: tenant with NO Razorpay and NO offline UPI → page shows
      "[Tenant] has been notified and will connect with you for the further
      steps" — no Pay button anywhere; Accept is replaced by the
      "Accepted on payment" notice; Reject still works
  [ ] RAZORPAY: tenant with Razorpay configured → "Pay ₹X now" opens the
      checkout popup; complete a test payment → success screen appears,
      invoice flips to paid, contract flips pending_acceptance → active
  [ ] OFFLINE UPI: tenant with offline UPI configured → VPA shows copyable +
      the uploaded QR renders; enter a reference, "I've paid" → "Payment
      reference submitted" state; second declare attempt says already pending
  [ ] TENANT CONFIRM (curl/Postman — no UI yet):
      GET /api/payments/declarations shows the pending row;
      POST /api/payments/declarations/:id/confirm → invoice pays + contract
      activates; {confirm:false} on a fresh declaration → rejected
  [ ] SERVER GATE: POST /api/contracts/public/respond with action=accept on an
      unpaid payment-gated contract → 400 PAYMENT_REQUIRED (button is hidden
      in the UI, this proves the backstop)
  [ ] REGRESSION: a signoff (non-payment) contract's review page still shows
      the normal Accept Contract button and accepting works

⚠️ DO NOT RUN STEPS 4-11 BELOW UNTIL THE ABOVE IS CONFIRMED WORKING.
Per this repo's CLAUDE.md: no submodule commit, no merge, until you say
"tested, working — proceed with merge."

═══════════════════════════════════════════════════
🚀 PHASE 2 — ONLY AFTER YOU CONFIRM TESTING PASSED
═══════════════════════════════════════════════════

STEP 4: Commit API Changes
─────────────────────────────────
cd contractnest-api
git status
git add src/services/publicPaymentService.ts src/services/integrationService.ts src/controllers/contractController.ts src/controllers/paymentGatewayController.ts src/controllers/integrationController.ts src/routes/contractRoutes.ts src/routes/paymentGatewayRoutes.ts src/routes/integrationRoutes.ts
git commit -m "feat: CNAK public payment collection + declaration confirm + QR upload"
git push origin main
cd ..

STEP 5: Commit UI Changes
─────────────────────────────────
cd contractnest-ui
git status
git add src/hooks/useIntegrations.ts src/components/integrations/DynamicFormField.tsx src/services/serviceURLs.ts
git commit -m "feat: QR image upload field for config-only integrations (offline UPI)"
git push origin main
cd ..

STEP 6: Commit Edge Changes (docs only — migrations already applied live)
─────────────────────────────────
cd contractnest-edge
git status
git add supabase/migrations/business-model-v2/031_generalize_payment_gated_entitlements.sql supabase/migrations/business-model-v2/032_public_payment_collection.sql supabase/migrations/business-model-v2/033_fix_record_invoice_payment_acceptance_method.sql supabase/migrations/business-model-v2/034_offline_upi_qr_and_declaration_listing.sql
git commit -m "docs: source-of-record migrations 031-034 (already applied live)"
git push origin main
cd ..

STEP 7-9: FamilyKnows / ContractNest-Mobile / ClaudeDocumentation
─────────────────────────────────
Not affected — no changes in this batch. Skip.

STEP 10: Update Parent Repo Submodule References
─────────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"
git add contractnest-api contractnest-ui contractnest-edge
git commit -m "chore: update submodules — CNAK public payment collection"
git push origin claude/init-submodules-2qx34t

STEP 11: Merge to Master
─────────────────────────────────
git checkout master
git pull origin master
git merge claude/init-submodules-2qx34t

# If merge conflicts in submodules:
cd [conflicted-submodule]
git checkout main
git pull origin main
cd ..
git add [conflicted-submodule]

git commit -m "Merge claude/init-submodules-2qx34t — CNAK public payment collection"
git push origin master

STEP 12: Verify Everything is Clean
─────────────────────────────────
git status
git submodule status
# Should show: "nothing to commit, working tree clean"

STEP 13: Restart Dev Server & Final Retest
─────────────────────────────────
cd contractnest-api && npm run dev
cd ..\contractnest-ui && npm run dev
# Hard refresh browser: Ctrl+F5
