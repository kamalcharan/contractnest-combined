COPY INSTRUCTIONS — kt-build-tree
==================================
Branch: claude/fix-industry-equipment-pricing-yHxIw
Steps Complete: Step 1 (skill file) + Step 2 (API endpoint)
Steps Pending:  Step 3 (Frontend hook) + Step 4 (Wire button)

FILES CREATED/MODIFIED (contractnest-api):
───────────────────────────────────────────
NEW  src/skills/kt-equipment-generator.md           ← LLM system prompt (Step 1)
NEW  src/services/knowledgeTreeGeneratorService.ts  ← calls Anthropic API
NEW  src/controllers/knowledgeTreeController.ts     ← validates input, handles response
NEW  src/routes/knowledgeTreeRoutes.ts              ← POST /api/knowledge-tree/generate
MOD  src/index.ts                                   ← registers route

DESTINATION (on your local machine):
─────────────────────────────────────
D:\projects\core projects\ContractNest\contractnest-combined\contractnest-api\

API ENDPOINT:
─────────────
POST /api/knowledge-tree/generate
Headers: Authorization: Bearer <token>, x-tenant-id: <tenantId>
Body:
{
  "equipmentName": "Elevator / Lift",
  "subCategory": "Vertical Transport",
  "resourceTemplateId": "uuid-from-db",
  "serviceActivity": "pm"          ← optional, defaults to "pm"
}
Response: { "success": true, "data": { ...full KT JSON payload... } }

ENV VAR REQUIRED:
──────────────────
Add to contractnest-api/.env:
  ANTHROPIC_API_KEY=sk-ant-...

COPY COMMANDS (PowerShell):
────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"

# Step 1 — skill file (if not already copied)
New-Item -ItemType Directory -Force -Path "contractnest-api\src\skills"
Copy-Item "MANUAL_COPY_FILES\kt-build-tree\contractnest-api\src\skills\kt-equipment-generator.md" `
  -Destination "contractnest-api\src\skills\kt-equipment-generator.md" -Force

# Step 2 — API endpoint
Copy-Item "MANUAL_COPY_FILES\kt-build-tree\contractnest-api\src\services\knowledgeTreeGeneratorService.ts" `
  -Destination "contractnest-api\src\services\knowledgeTreeGeneratorService.ts" -Force
Copy-Item "MANUAL_COPY_FILES\kt-build-tree\contractnest-api\src\controllers\knowledgeTreeController.ts" `
  -Destination "contractnest-api\src\controllers\knowledgeTreeController.ts" -Force
Copy-Item "MANUAL_COPY_FILES\kt-build-tree\contractnest-api\src\routes\knowledgeTreeRoutes.ts" `
  -Destination "contractnest-api\src\routes\knowledgeTreeRoutes.ts" -Force
Copy-Item "MANUAL_COPY_FILES\kt-build-tree\contractnest-api\src\index.ts" `
  -Destination "contractnest-api\src\index.ts" -Force

Write-Host "Steps 1+2 done — restart API server" -ForegroundColor Green
