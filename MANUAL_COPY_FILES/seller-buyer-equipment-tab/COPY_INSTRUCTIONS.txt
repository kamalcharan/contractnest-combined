===============================================================
EQUIPMENT TAB — Contact-Based Filter Fix + Reuse Cleanup
===============================================================

Feature: EquipmentTab rewritten to reuse EquipmentCard (selectable)
         + EquipmentFormDialog from equipment-registry. Fixed contact
         filter so picker shows ONLY equipment for the buyer/contact
         on this specific contract (Revenue side).

Submodules Affected: contractnest-ui ONLY

===============================================================
FILES TO COPY (2 files)
===============================================================

-- contractnest-ui (2 files) --

1. src/components/contracts/EquipmentTab.tsx
   -> Fully rewritten: reuses EquipmentCard + EquipmentFormDialog
   -> Removed ALL orphan code (old inline AssetPicker, duplicate card,
     duplicate CONDITION_CONFIG/CRITICALITY_CONFIG constants)
   -> Added buyerId prop for contact-based registry filtering
   -> Revenue side (seller): ownership_type='client' + contact_id=buyerId
   -> Expense side (buyer): ownership_type='self'

2. src/pages/contracts/detail/index.tsx
   -> Passes buyerId={contract.buyer_contact_person_id || contract.contact_id}
   -> Previously used contract.buyer_id (tenant/org ID -- WRONG)

===============================================================
NO DB MIGRATION REQUIRED
===============================================================
