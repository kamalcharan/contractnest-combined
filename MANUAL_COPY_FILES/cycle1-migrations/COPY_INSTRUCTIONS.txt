============================================================================
SmartForms — Cycle 1 Step 1: Database Migration (with m_form_ prefix)
============================================================================

SUBMODULES AFFECTED: contractnest-edge

FILES:
  1. contractnest-edge/supabase/migrations/smart-forms/001_create_smart_forms_tables.sql
     → Full schema with m_form_ prefix (for fresh installs)

  2. contractnest-edge/supabase/migrations/smart-forms/002_rename_tables_m_form_prefix.sql
     → ALTER RENAME migration (for existing DBs that already ran original migration)

TABLE NAME MAPPING:
  form_templates          → m_form_templates
  tenant_form_selections  → m_form_tenant_selections
  form_template_mappings  → m_form_template_mappings
  form_submissions        → m_form_submissions
  form_attachments        → m_form_attachments

FOR EXISTING DB (already ran original migration):
  Run ONLY: 002_rename_tables_m_form_prefix.sql

FOR FRESH DB:
  Run ONLY: 001_create_smart_forms_tables.sql

============================================================================
