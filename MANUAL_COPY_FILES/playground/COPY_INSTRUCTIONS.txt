═══════════════════════════════════════════════════
📦 PLAYGROUND - LANDING PAGE SECTION (UPDATED v2)
═══════════════════════════════════════════════════
Feature: Interactive Playground Section within Landing Page
(NOT a separate page - integrated into landing page)

NEW FILE:
  - contractnest-ui/src/components/landing/LandingPlayground.tsx

FILE TO MODIFY:
  - contractnest-ui/src/pages/public/LandingPage.tsx

Submodules Affected: contractnest-ui

FIXES IN THIS VERSION:
  - Added Email field (required)
  - Added proper validation for all fields
  - All fields marked mandatory with * indicator
  - Fixed Supabase integration with proper error logging
  - Fixed form reset to include email

═══════════════════════════════════════════════════
🗄️ SUPABASE SETUP (DO THIS FIRST!)
═══════════════════════════════════════════════════

Go to Supabase Dashboard → SQL Editor and run:

CREATE TABLE IF NOT EXISTS leads_contractnest (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  industry VARCHAR(100) DEFAULT 'equipment_amc',
  persona VARCHAR(20) NOT NULL,
  completed_demo BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE leads_contractnest ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow anonymous inserts" ON leads_contractnest
  FOR INSERT TO anon WITH CHECK (true);

-- Also add select policy so .select() works after insert
CREATE POLICY "Allow anonymous reads" ON leads_contractnest
  FOR SELECT TO anon USING (true);

═══════════════════════════════════════════════════
💻 COPY & INTEGRATE COMMANDS
═══════════════════════════════════════════════════

STEP 1: Navigate to Project
─────────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"

STEP 2: Copy LandingPlayground Component
─────────────────────────────────
Copy-Item "MANUAL_COPY_FILES\playground\contractnest-ui\src\components\landing\LandingPlayground.tsx" -Destination "contractnest-ui\src\components\landing\" -Force

Write-Host "✅ LandingPlayground.tsx copied!" -ForegroundColor Green

STEP 3: Edit LandingPage.tsx (MANUAL)
─────────────────────────────────
Open: contractnest-ui/src/pages/public/LandingPage.tsx

ADD IMPORT (around line 18, after LandingFooter):
import LandingPlayground from '../../components/landing/LandingPlayground';

ADD COMPONENT (around line 565, AFTER </TheProblem> and BEFORE <DualPersonaTimeline):

        {/* Playground Section - Interactive Demo */}
        <LandingPlayground />

        {/* Dual Persona Timeline - NEW! */}
        <DualPersonaTimeline

The CORRECT ORDER should be:
  1. <TheProblem ... />         (line ~558-565)
  2. <LandingPlayground />      <-- ADD HERE
  3. <DualPersonaTimeline ...   (currently line ~590)
  4. <LandingPricing ...

STEP 4: Test
─────────────────────────────────
cd contractnest-ui
npm run dev

Open: http://localhost:5173
Scroll down OR click Hero "Try the Playground" button

═══════════════════════════════════════════════════
🧪 TESTING CHECKLIST
═══════════════════════════════════════════════════

HERO:
- [ ] Button scrolls to #playground section

PLAYGROUND CTA:
- [ ] 🎮 icon and "Playground" title
- [ ] Workflow visual (4 steps)
- [ ] "See For Yourself - Try Now" button

USER FORM:
- [ ] Mobile (+91) field with validation
- [ ] Email field with validation
- [ ] Name field with validation
- [ ] Company field with validation
- [ ] All fields show * for required
- [ ] Validation error messages appear
- [ ] "Start Building" advances only when valid

SUPABASE:
- [ ] Open browser console (F12 → Console)
- [ ] Submit form and check for:
      "Lead saved successfully: [...]" OR
      "Supabase Error: [...]"
- [ ] Check Supabase table for new record

BUILDER:
- [ ] Customer Name input
- [ ] 4 block categories
- [ ] Drag-drop (desktop) / Tap-to-add (mobile)
- [ ] Total updates
- [ ] "Send Contract" works

SUCCESS:
- [ ] Download HTML works
- [ ] "Start Free Trial" → /register
- [ ] "Try Again" restarts

═══════════════════════════════════════════════════
⏸️ CONFIRM AFTER TESTING
═══════════════════════════════════════════════════
  → "Tested, working"
  → "Issue found: [describe]"
═══════════════════════════════════════════════════
