═══════════════════════════════════════════════════
📦 EDGE LAYER V2 RPCs - CHANGES SUMMARY
═══════════════════════════════════════════════════
Branch: edge-v2-rpcs
Date: 2026-01-14

Files Changed:
  - contractnest-edge/supabase/functions/_shared/contacts/contactService.ts
    → Uses v2 RPCs with embedded relations and idempotency
  - contractnest-edge/supabase/functions/contacts/index.ts
    → Handles x-idempotency-key header for retry-safe operations

Submodules Affected: contractnest-edge

═══════════════════════════════════════════════════
🔄 V2 RPC MAPPING
═══════════════════════════════════════════════════
OLD RPC                        → NEW V2 RPC
─────────────────────────────────────────────────
list_contacts_filtered         → list_contacts_with_channels_v2
(multiple queries for data)    → get_contact_full_v2
create_contact_transaction     → create_contact_idempotent_v2
update_contact_transaction     → update_contact_idempotent_v2
─────────────────────────────────────────────────

═══════════════════════════════════════════════════
✅ PRODUCTION CHECKLIST
═══════════════════════════════════════════════════
  ✅ Transaction Management: Yes - handled by RPCs
  ✅ Race Condition Handling: Yes - idempotency keys prevent duplicates
  ✅ Error Handling: Yes - try-catch with proper error codes
  ✅ Single DB Call: Yes - v2 RPCs return embedded relations
  ✅ No Loops in Edge: Yes - all aggregation in Postgres

═══════════════════════════════════════════════════
💻 PHASE 1: COPY FILES FOR LOCAL TESTING
═══════════════════════════════════════════════════

STEP 1: Navigate to Project (PowerShell)
─────────────────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"

STEP 2: Copy Files from MANUAL_COPY_FILES
─────────────────────────────────────────
Copy-Item "MANUAL_COPY_FILES\edge-v2-rpcs\contractnest-edge\*" -Destination "contractnest-edge\" -Recurse -Force

Write-Host "✅ Edge layer files copied!" -ForegroundColor Green

STEP 3: Deploy Edge Function to Supabase
─────────────────────────────────────────
cd contractnest-edge
supabase functions deploy contacts

═══════════════════════════════════════════════════
🧪 TESTING CHECKLIST
═══════════════════════════════════════════════════
- [ ] List contacts - verify embedded primary_channel/primary_address
- [ ] Get contact by ID - verify all relations (channels, addresses, parent_contacts, contact_persons)
- [ ] Create contact - test idempotency (same x-idempotency-key returns same contact)
- [ ] Update contact - test idempotency (same x-idempotency-key doesn't re-update)
- [ ] Test with x-environment: test header - verify is_live=false
- [ ] Test with x-environment: live header - verify is_live=true

═══════════════════════════════════════════════════
📋 NEW HEADER SUPPORT
═══════════════════════════════════════════════════
Header: x-idempotency-key (Optional)
Type: UUID
Purpose: Retry-safe create/update operations

Usage:
  - If provided: Uses this key for idempotency check
  - If not provided: Auto-generates a new UUID

Example:
  curl -X POST .../contacts \
    -H "x-tenant-id: your-tenant-uuid" \
    -H "x-idempotency-key: $(uuidgen)" \
    -d '{"name": "John", ...}'

═══════════════════════════════════════════════════
⏸️ WAITING FOR CONFIRMATION
═══════════════════════════════════════════════════
Test locally and confirm:
  → "Tested, working - proceed with commit"
  → "Issue found: [describe problem]"
═══════════════════════════════════════════════════
