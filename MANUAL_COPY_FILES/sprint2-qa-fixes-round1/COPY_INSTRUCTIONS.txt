═══════════════════════════════════════════════════
🔧 UPDATE 2 — Catalog Studio blocks list ignored the Test/Live toggle
═══════════════════════════════════════════════════
Reported: "Saturday Cadence" (a Group Session block) visible while
creating a template in Test environment, not in Live.

ROOT CAUSE (traced end to end): the frontend's Live/Test toggle
(localStorage) is correctly sent as an x-environment header on every
request. contractnest-api's catalogStudioController correctly reads it.
But catBlocksService.makeRequest() — the function that forwards the
request to the cat-blocks edge function — builds its headers from
scratch and never includes x-environment. The edge function's
extractRequestContext() defaults environment to 'live' whenever the
header is missing, so ctx.isLive was ALWAYS true for every blocks-list
request, system-wide — the toggle had zero effect on this endpoint.

DECISION (confirmed): rather than fix the toggle to make Test/Live
legitimately diverge, Catalog Studio should show the SAME data in both
environments — matching how /settings/configure/resources already has
no environment split at all. Applies globally (all tenants), since the
toggle was already non-functional, not working-as-designed.

THE FIX — contractnest-edge/supabase/functions/cat-blocks/index.ts,
handleGetBlocks (the tenant-facing list handler; handleGetBlocksAdmin is
untouched):
  - Removed the `.eq('is_live', ctx.isLive)` filter entirely.
  - Because bulk-seed creates a separate test row AND live row per
    logical block (same tenant + resource_template_id + name, different
    id), just dropping the filter would make every seeded block appear
    TWICE. Added a dedup pass: fetch all candidate rows (capped at 2000,
    well above any tenant's realistic count), collapse by
    (tenant_id, resource_template_id, name) preferring the live copy
    when both exist, THEN apply the requested page/limit to the deduped
    list. Pagination's total/has_more now reflect the deduped count.
  - ⚠️ NOT YET DEPLOYED — this is a shared platform edge function, say
    the word and I'll deploy + verify byte-for-byte, same as every other
    edge change this session.

Two follow-ups from live testing:

1. The "Sync Prices from KT" button was only reachable from the equipment
   detail page's VaNi card — nothing in the sidebar (where the UNPRICED
   badge actually lives) hinted the action existed. Added a small inline
   ↻ control directly beside the UNPRICED badge in the sidebar itself.

2. User asked what "Re-seed with VaNi" actually does, and testing showed
   it's a no-op on any already-seeded equipment ("Already in your catalog
   — nothing new to load") — its idempotency check is whole-template (any
   existing row -> skip entirely), so it can never pull in new items the
   KT has gained since the tenant first seeded. That made the pricing fix
   incomplete: syncing prices only helps items that already exist in the
   tenant's catalog, not items the KT added later that never got seeded
   at all. So the reprice function became a proper sync function:

   ktCatBlockMapperService.repriceBlocksForTemplate() -> renamed to
   syncBlocksForTemplate() — now does BOTH: adds any KT block missing
   from the tenant's catalog by name (both test + live), and backfills
   base_price on existing-but-unpriced blocks. Never touches anything the
   tenant has already priced or customized. Route renamed to match:
   POST /api/seeds/tenant/sync-equipment (was reprice-equipment).

   On the equipment detail page, the VaNi card's action for BOTH 'seeded'
   and 'unpriced' states now calls this same sync — "Re-seed with VaNi"
   is gone; it's "⟳ Sync with Knowledge Tree" everywhere except first-time
   seeding ('kt' state), which still goes through the preview-then-load
   flow since that's a bigger, first-time action worth previewing.

═══════════════════════════════════════════════════
📦 CHANGES SUMMARY
═══════════════════════════════════════════════════
Branch: sprint2-qa-fixes-round1
(Fixes for 3 issues found while QA-testing the auto-generate-kt-forms +
equipment-filter batches on a live "sharma elevators" onboarding)

═══════════════════════════════════════════════════
1) KT-generated SmartForms auto-approve (were invisible to tenants)
═══════════════════════════════════════════════════
Root cause: /settings/configure/smart-forms only lists status='approved'
templates. Every KT-auto-generated form was created as 'draft' and
nothing ever moved it forward — so equipment with a fully-seeded catalog
showed zero forms in the tenant-facing Smart Forms page. This was PRD
Section 13 Open Decision #2 ("agent forms go draft->review->approved OR
skip to approved"), left open. Resolved: auto-approve, since these forms
are deterministically composed from vetted Knowledge Tree checkpoint
data, not free-form LLM output.

  - contractnest-api/src/services/ktServiceFormGeneratorService.ts
      generateFormsForTemplate's insert now sets status: 'approved' (was
      'draft'), plus approved_by/approved_at/review_notes for an audit
      trail. Also added getFormStatusForTemplate() — read-only, powers #2.

═══════════════════════════════════════════════════
2) Admin KT tree page showed duplicate forms (one row per tenant)
═══════════════════════════════════════════════════
Root cause: the "Per-Service Forms" panel on the admin KT tree page
queried m_cat_blocks (tenant-owned — one copy per tenant that seeded the
equipment) filtered only by resource_template_id, no tenant grouping. With
N tenants seeded, admins saw N duplicate rows per service — the exact
"14 rows" issue from earlier this session, just not yet fixed in this
specific panel. Fix: this panel now reads from m_kt_service_form_map (the
platform-level mapping table already built for onboarding auto-attach)
instead of m_cat_blocks — one row per service, period, regardless of how
many tenants have seeded the equipment.

  - contractnest-api/src/services/ktCatBlockMapperService.ts
      New getFormStatusForTemplate() [via ktServiceFormGeneratorService,
      see above] reads m_kt_service_form_map + m_form_templates status.
  - contractnest-api/src/controllers/knowledgeTreeController.ts
      New getServiceFormStatus handler — GET /generate-forms's read-only
      sibling.
  - contractnest-api/src/routes/knowledgeTreeRoutes.ts
      New route: GET /api/knowledge-tree/service-forms?resourceTemplateId=
  - contractnest-ui/.../knowledge-tree/components/FormPreviewTab.tsx
      "Per-Service Forms" panel rewritten: fetches the new endpoint
      instead of useCatBlocks(), shows one row per service group with a
      status badge (form ready / no form yet), plus a single "Generate
      Missing" button (calls the existing generate-forms endpoint, which
      already skips groups that have a form). The old per-block "Generate
      & Attach" button is gone — forms are created automatically right
      after Service Names now, this panel is a status view + fallback.

═══════════════════════════════════════════════════
3) Already-seeded equipment stayed "unpriced" after generating KT prices
═══════════════════════════════════════════════════
Root cause, confirmed via direct DB query on sharma@s.com's tenant:
Elevator's 35 spare parts are fully priced in the knowledge tree RIGHT
NOW, but every one of the tenant's 70 seeded blocks (test+live) has
base_price null. Their multi-currency pricing table rows (m_kt_prices)
were created at 11:14:20 today — 44 minutes AFTER their blocks were
seeded at 10:30:56. Seeding is a one-time snapshot copy, not a live
reference, and there was no mechanism to push updated KT prices into
already-seeded blocks. Separately confirmed "Re-seed with VaNi" — despite
its "re-running is safe" copy — actually no-ops entirely on already-seeded
equipment (the bulk-seed idempotency check finds existing rows and skips),
so it can't fix this either.

  - contractnest-api/src/services/ktCatBlockMapperService.ts
      New repriceBlocksForTemplate(tenantId, resourceTemplateId, authToken):
      recomputes fresh blocks from current KT data, matches the tenant's
      existing is_seed=true blocks by name, and updates base_price/
      currency/config ONLY for rows that are still at base_price null/0
      (anything the tenant already priced or edited is left untouched).
  - contractnest-api/src/routes/seedRoutes.ts
      New route: POST /api/seeds/tenant/reprice-equipment
      Body: { resourceTemplateId }
  - contractnest-ui/src/pages/catalog-studio/equipment.tsx
      When an equipment's state is 'unpriced', the VaNi card CTA now
      reads "Sync Prices from KT" and calls the new reprice endpoint
      instead of the seed flow (which would just no-op). 'seeded'/'kt'
      states are unchanged.

Submodules Affected: contractnest-api, contractnest-ui
(No new migration, no edge redeploy — this round is API + UI only.)

Production Checklist:
  ✅ Transaction Management: reprice updates are per-row (one UPDATE per
     block), matching the existing pattern in this file; a failure on one
     block is logged and skipped, doesn't abort the rest
  ✅ Race Condition Handling: reprice only touches rows still at their
     original unpriced seed state (base_price null/0) — safe to run
     repeatedly, and never clobbers a price the tenant has since set
  ✅ Error Handling: all new endpoints follow the same try/catch +
     structured error response pattern as every other route in these files
  ✅ Toasts: syncPrices()/handleGenerateMissing() both toast success
     (with count) or a clear "nothing to do" info toast
  ✅ Loaders: both new actions have dedicated loading state (button
     disables + spinner) distinct from the existing seed-preview flow

═══════════════════════════════════════════════════
💻 PHASE 1: COPY FILES FOR LOCAL TESTING
═══════════════════════════════════════════════════

STEP 1: Navigate to Project
─────────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"

STEP 2: Copy Files from MANUAL_COPY_FILES
─────────────────────────────────
NOTE: apply sprint2-auto-generate-kt-forms and sprint2-arm-catalog-studio
first if you haven't already — this batch edits files those introduced.

Copy-Item "MANUAL_COPY_FILES\sprint2-qa-fixes-round1\contractnest-api\*" -Destination "contractnest-api\" -Recurse -Force
Copy-Item "MANUAL_COPY_FILES\sprint2-qa-fixes-round1\contractnest-ui\*" -Destination "contractnest-ui\" -Recurse -Force

Write-Host "✅ Files copied!" -ForegroundColor Green

STEP 3: Restart affected dev servers
─────────────────────────────────
cd contractnest-api
npm run dev

(In another terminal)
cd contractnest-ui
npm run dev
# Hard refresh browser: Ctrl+F5

═══════════════════════════════════════════════════
🧪 TESTING CHECKLIST
═══════════════════════════════════════════════════
- [ ] Run "Generate Service Names" on a fresh (or existing) equipment —
      confirm the resulting forms show up on /settings/configure/smart-forms
      immediately (no manual approve step)
- [ ] Open the admin KT tree page for an equipment seeded by multiple
      tenants (e.g. Elevator / Lift) — confirm "Per-Service Forms" shows
      ONE row per service, not one per tenant
- [ ] Click "Generate Missing" on a service group with "no form yet" —
      confirm it creates the form and the row flips to "approved"
- [ ] Go to /catalog-studio/equipment — an UNPRICED equipment now shows a
      small ↻ button right next to the badge in the sidebar itself (not
      just on the detail page). Click it — confirm it actually updates
      the spare/service rows to show real prices.
- [ ] Open an equipment already fully "IN CATALOG" — confirm its card now
      says "⟳ Sync with Knowledge Tree" (not "Re-seed with VaNi") and
      clicking it reports "Already up to date" rather than the old
      "Already in your catalog — nothing new to load"
- [ ] Re-click sync a second time on the same equipment — confirm it
      reports nothing to do, rather than erroring or double-applying
- [ ] (After the cat-blocks edge deploy) At /catalog-studio/templates-list,
      create/edit a template — confirm "Saturday Cadence" (or any block)
      shows up identically whether the Live/Test toggle is set to Live or
      Test
- [ ] Open an equipment with lots of seeded blocks (e.g. HVAC — 136 raw
      rows, ~68 after dedup) — confirm no duplicate rows appear in the
      block picker or in /catalog-studio/blocks, and pagination (if you
      page through) doesn't skip or repeat items

═══════════════════════════════════════════════════
⏸️ WAITING FOR CONFIRMATION
═══════════════════════════════════════════════════
Test locally and confirm:
  → "Tested, working - proceed with merge"
  → "Issue found: [describe problem]"
═══════════════════════════════════════════════════
