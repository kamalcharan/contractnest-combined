BILLING VIEW - Payment Schedule + In-page Header + As Defined
=============================================================

Changes:
1. Added in-page header ("Billing View" + subtitle) to match other wizard steps
2. Renamed "Payment Mode" to "Payment Schedule"
3. Added 3rd payment option "As Defined" — billed per each block's cycle
4. Conditional availability:
   - Upfront: only clickable if ALL blocks are prepaid (locked + dimmed otherwise)
   - EMI: only clickable if ALL blocks are postpaid (locked + dimmed otherwise)
   - As Defined: always available
5. "As Defined" shows breakup grouped by cycle type (Prepaid, Monthly, Postpaid, etc.)
6. Increased Column 3 width from 380px to 420px
7. Updated paymentMode type to include 'defined'

Files Modified (contractnest-ui only):
1. src/components/contracts/ContractWizard/index.tsx
   - paymentMode type: 'prepaid' | 'emi' | 'defined'
   - handlePaymentModeChange handler updated

2. src/components/contracts/ContractWizard/steps/BillingViewStep.tsx
   - Added in-page header
   - Payment Schedule section with 3 options
   - Conditional availability (allPrepaid / allPostpaid flags)
   - As Defined breakup (definedBreakup computed value)
   - Column 3 width 420px

Copy Commands (PowerShell):
Copy-Item "MANUAL_COPY_FILES\billing-view-payment-schedule\contractnest-ui\src\components\contracts\ContractWizard\index.tsx" -Destination "contractnest-ui\src\components\contracts\ContractWizard\index.tsx" -Force
Copy-Item "MANUAL_COPY_FILES\billing-view-payment-schedule\contractnest-ui\src\components\contracts\ContractWizard\steps\BillingViewStep.tsx" -Destination "contractnest-ui\src\components\contracts\ContractWizard\steps\BillingViewStep.tsx" -Force
