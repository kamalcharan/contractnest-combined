═══════════════════════════════════════════════════════════════════
📦 CHANGES SUMMARY — Wizard-testable V2, end to end
═══════════════════════════════════════════════════════════════════
Branch: claude/tenant-bbb-data-issues-buwxqm
Submodules Affected: contractnest-edge, contractnest-api, contractnest-ui

WHY THIS BATCH EXISTS: Sprints 2-3 only proved the RPC layer via direct
SQL — nothing reachable by a real request. This batch closes that gap:
create → API → edge → RPC, all wired, so contract creation via V2 can
actually be tested through the real Wizard, not just asserted by me.

─────────────────────────────────────────────────────────────────
NEW FILES (safe to copy directly — nothing to merge)
─────────────────────────────────────────────────────────────────
  - contractnest-edge/supabase/functions/contracts-v2/index.ts
    New edge function. POST only, mirrors contracts/index.ts's
    handleCreate but calls create_contract_transaction_v2. Already
    DEPLOYED live to the Supabase project (contracts/index.ts itself
    untouched). seller_id = tenant, buyer_id = body.buyer_id — same
    resolution V1 uses today; the Revenue/Expense-mode inversion found
    in Sprint 3 is NOT fixed here, deliberately (separate, parked item).

  - contractnest-api/src/services/contractServiceV2.ts   [NEW]
  - contractnest-api/src/controllers/contractControllerV2.ts   [NEW]
  - contractnest-api/src/routes/contractRoutesV2.ts   [NEW]
    Mirror contractService.ts / contractController.ts / contractRoutes.ts
    exactly (same HMAC signing, same header shape) — POST only, pointed
    at the contracts-v2 edge function instead of contracts.
    contractService.ts / contractController.ts / contractRoutes.ts are
    completely untouched.

─────────────────────────────────────────────────────────────────
MODIFIED FILES — delivered as FULL, ready-to-copy files, not diffs.
No manual merging needed: these ARE index.ts and useContractQueries.ts
in their entirety, with the two small additive changes already applied.
Overwrite your local copies directly — but note that means anything
else you've changed in either of these two files locally would be lost,
since this is a full-file replacement, not a patch.
─────────────────────────────────────────────────────────────────
  - contractnest-api/src/index.ts
    Two small additive blocks (a "Load Contract routes V2" block and a
    "Register Contract CRUD routes V2" block, both copy-pasted verbatim
    from the existing contractCrudRoutes pattern) are the only changes
    versus what was there before this batch — confirmed via grep before
    delivery (5 occurrences of contractCrudRoutesV2 present).

  - contractnest-ui/src/hooks/queries/useContractQueries.ts
    One change, inside useContractOperations()'s createContractMutation:
    reads `?useV2=1` from the URL. Absent (every real user, always),
    behavior is byte-identical to before — same endpoint, same
    everything. Present, that one submission posts to /api/v2/contracts
    instead. Confirmed via grep before delivery (3 occurrences of useV2
    present).

─────────────────────────────────────────────────────────────────
⚠️ WHAT I COULD AND COULDN'T VERIFY MYSELF
─────────────────────────────────────────────────────────────────
VERIFIED (real, executed, not just asserted):
  - create_contract_transaction_v2 RPC — proven via direct SQL, Sprints 2-3.
  - contracts-v2 edge function — deployed successfully, Supabase confirms
    ACTIVE status, no bundle errors.

NOT VERIFIED FROM THIS SESSION — this sandbox has no .env for
contractnest-api (no SUPABASE_SERVICE_ROLE_KEY / INTERNAL_SIGNING_SECRET),
so I could not run the API server or curl it myself. Outbound HTTPS to
Supabase's function endpoint is also proxy-blocked from here. The API
layer (contractServiceV2/contractControllerV2/contractRoutesV2) is
written to mirror V1's exact pattern, but the first real end-to-end test
happens when you run this locally and hit the Wizard with ?useV2=1.

─────────────────────────────────────────────────────────────────
💻 STEP 1: Copy files into your local checkout
─────────────────────────────────────────────────────────────────
cd "D:\projects\core projects\ContractNest\contractnest-combined"

New-Item -ItemType Directory -Force -Path "contractnest-edge\supabase\functions\contracts-v2" | Out-Null
Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-edge\supabase\functions\contracts-v2\index.ts" -Destination "contractnest-edge\supabase\functions\contracts-v2\index.ts" -Force

New-Item -ItemType Directory -Force -Path "contractnest-edge\supabase\migrations\contracts-v2" | Out-Null
Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-edge\supabase\migrations\contracts-v2\*.sql" -Destination "contractnest-edge\supabase\migrations\contracts-v2\" -Force

Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-api\src\services\contractServiceV2.ts" -Destination "contractnest-api\src\services\contractServiceV2.ts" -Force
Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-api\src\controllers\contractControllerV2.ts" -Destination "contractnest-api\src\controllers\contractControllerV2.ts" -Force
Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-api\src\routes\contractRoutesV2.ts" -Destination "contractnest-api\src\routes\contractRoutesV2.ts" -Force

Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-api\src\index.ts" -Destination "contractnest-api\src\index.ts" -Force
Copy-Item "MANUAL_COPY_FILES\manual_copy_v2\contractnest-ui\src\hooks\queries\useContractQueries.ts" -Destination "contractnest-ui\src\hooks\queries\useContractQueries.ts" -Force

Write-Host "✅ All files copied — no manual merging needed." -ForegroundColor Green

─────────────────────────────────────────────────────────────────
STEP 2: Test via the real Wizard
─────────────────────────────────────────────────────────────────
1. Restart contractnest-api locally (picks up the new route).
2. Open the Contract Wizard with ?useV2=1 appended to the URL.
3. Create a contract on your new test tenant, normally, through the UI.
4. Verify in Supabase: the contract's t_contract_access row exists
   unconditionally, and a matching n_jtd row exists with
   source_type_code='contract_created' and the correct is_live.

STEP 3: Commit + push (your call, whenever you're ready) — same as
every prior batch, nothing pushed by me.

─────────────────────────────────────────────────────────────────
🧪 TESTING CHECKLIST
─────────────────────────────────────────────────────────────────
- [ ] Wizard with ?useV2=1 successfully creates a contract
- [ ] Wizard WITHOUT ?useV2=1 behaves exactly as before (regression check)
- [ ] CNAK grant row created unconditionally
- [ ] n_jtd row created, correct is_live, source_type_code='contract_created'
- [ ] contracts/index.ts, contractController.ts, contractService.ts,
      contractRoutes.ts, useContractQueries.ts's non-createContract code —
      all unchanged
═══════════════════════════════════════════════════════════════════
