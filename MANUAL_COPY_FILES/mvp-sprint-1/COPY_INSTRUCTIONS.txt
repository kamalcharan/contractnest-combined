═══════════════════════════════════════════════════
COPY INSTRUCTIONS — MVP Sprint 1
Branch: claude/contractnest-launch-strategy-aad14f
Date:   28 Jul 2026
═══════════════════════════════════════════════════

SUBMODULES AFFECTED
───────────────────
  contractnest-ui  (branch: main)   <-- ONLY this one

  No API, edge, migration or table changes. UX-only, per EXECUTION-PLAN rule 1.

WHAT THIS BATCH IS
──────────────────
  All four Sprint 1 items:
    1. New public landing page (replaces the old one at "/")
    2. src/lite scaffold
    3. reveal (hide-until-earned) module
    4. one-way import rules that keep src/lite deletable

  Per your decision: the new landing REPLACES the previous 19-component landing.
  The old files are left in the tree, just unwired — delete them once this has
  run a week in production. Also per your decision, all testimonial and
  social-proof content is GONE; the proof band now carries only claims we can
  support (39 chapters, 4 trades, 15 minutes, free under 10 contracts).

FILES  (13 new, 2 modified)
───────────────────────────
  LANDING (new)
    src/lite/landing/LandingPage.tsx    Page composition. Carries the visitor's
                                        trade into signup via localStorage so
                                        onboarding never asks for it again.
    src/lite/landing/HeroDemo.tsx       3-scene loop: write → CNAK → accepted,
                                        clock landing on 3:00. Starts on
                                        scroll-into-view; shows the final signed
                                        state (no motion) under
                                        prefers-reduced-motion.
                                        SWAP SLOT: replace the contents of
                                        .cn-demostage with a muted autoplay
                                        <video> when the recording exists.
    src/lite/landing/WorkspaceSlab.tsx  The live workspace preview.
    src/lite/landing/TradePicker.tsx    Four trade chips.
    src/lite/landing/previewData.ts     Per-trade preview data (illustrative
                                        marketing data, NOT the seeder).
    src/lite/landing/sections.tsx       Nav, 15-minute strip, value cards,
                                        client-portal split, proof band, FAQ,
                                        final CTA, footer.
    src/lite/landing/landing.css        All styles. Every class in the cn-
                                        namespace, tokens on .cn-landing.
                                        Independent of ThemeContext by design.

  REVEAL (new)
    src/lite/reveal/revealRules.ts      9 rules as DATA; each fails OPEN on
                                        unknown signals.
    src/lite/reveal/revealContext.ts    Context + accessor.
    src/lite/reveal/RevealProvider.tsx  Holds signals; fetches nothing.
                                        revealAll={true} shows everything —
                                        pass it for pre-MVP tenants.
    src/lite/reveal/useReveal.ts        useReveal('perspective') -> boolean.
    src/lite/reveal/RevealGate.tsx      <RevealGate id="charts">…</RevealGate>

  MODIFIED
    src/App.tsx        Two lines: import LiteLandingPage from './lite/landing/
                       LandingPage', and SmartHomePage returns <LiteLandingPage/>
                       instead of <LandingPage/>. Nothing else touched.
    eslint.config.js   Two no-restricted-imports blocks. src/App.tsx is the one
                       documented exception (the single mount seam).

VERIFIED IN THE RUNNING APP BEFORE HANDOVER
───────────────────────────────────────────
  * Ran the real dev server and rendered the page — full page screenshotted,
    all sections correct, demo animating, trade chips and CTAs styled.
  * tsc --strict on src/lite              -> 0 errors
  * tsc app-wide (tsconfig.app.json)      -> 1188 errors, ALL pre-existing,
                                             0 from src/lite
  * eslint on src/lite and src/App.tsx    -> 0 new problems
                                             (App.tsx's 4 unused-var errors are
                                             pre-existing — confirmed against
                                             the pristine file)
  * eslint canary test                    -> both isolation rules fired
  * Browser console on the live page      -> no errors
  * contractnest-ui working tree left pristine (git status clean)

  Three real bugs were found by rendering it and fixed before handover:
    - the landing inherited the app shell's background (mockup had it on body)
    - a global :focus-visible and a global reduced-motion reset were leaking
      out of the stylesheet into the whole app
    - `.cn-landing button` (0,1,1) outranked `.cn-btn-cta` (0,1,0) and stripped
      every button; resets now use :where() at zero specificity

POWERSHELL — copy
─────────────────
  New-Item -ItemType Directory -Force -Path "contractnest-ui\src\lite"
  Copy-Item "MANUAL_COPY_FILES\mvp-sprint-1\contractnest-ui\src\lite\*" -Destination "contractnest-ui\src\lite\" -Recurse -Force
  Copy-Item "MANUAL_COPY_FILES\mvp-sprint-1\contractnest-ui\src\App.tsx" -Destination "contractnest-ui\src\App.tsx" -Force
  Copy-Item "MANUAL_COPY_FILES\mvp-sprint-1\contractnest-ui\eslint.config.js" -Destination "contractnest-ui\eslint.config.js" -Force

  Write-Host "Copied." -ForegroundColor Green

TEST
────
  cd contractnest-ui
  npm run dev
  # open http://localhost:5173  while LOGGED OUT  (use an incognito window)

  TESTING CHECKLIST
  [ ] New landing appears at / when logged out
  [ ] Hero demo cycles: write -> CNAK -> accepted, clock reaches 3:00, loops
  [ ] All four trade chips rebuild the slab (catalog count, contract, year strip)
  [ ] "See my workspace" scrolls to the trade picker
  [ ] "Start free with this setup" -> /signup
  [ ] Logged IN, / still redirects to /ops/cockpit as before
  [ ] Rest of the app looks unchanged (reveal is NOT wired to any screen yet)
  [ ] npm run lint -> no new errors from src/lite
  [ ] Mobile width: hero stacks, nav links hide, no horizontal scroll

NOT YET DONE (next batch, after your sign-off)
──────────────────────────────────────────────
  * Mount <RevealProvider> in App.tsx (revealAll for existing tenants)
  * Wrap the 9 surfaces with <RevealGate>
  * Delete the retired landing components once this has run a week
═══════════════════════════════════════════════════
