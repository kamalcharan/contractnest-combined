═══════════════════════════════════════════════════════════════════════════════
SMARTPROFILE API LAYER - COPY INSTRUCTIONS
═══════════════════════════════════════════════════════════════════════════════

This folder contains the SmartProfile API layer routes, controllers, and services.

FILES TO COPY:
══════════════

1. contractnest-api/src/groupsService.ts
   → Copy to: contractnest-api/src/groupsService.ts
   Purpose: Added SmartProfile service methods (getSmartProfile, saveSmartProfile,
            generateSmartProfile, searchSmartProfiles)

2. contractnest-api/src/groupsController.ts
   → Copy to: contractnest-api/src/groupsController.ts
   Purpose: Added SmartProfile controller handlers for Express routes

3. contractnest-api/src/routes/groupsRoutes.ts
   → Copy to: contractnest-api/src/routes/groupsRoutes.ts
   Purpose: Added SmartProfile routes:
            - GET /api/smartprofiles/:tenantId
            - POST /api/smartprofiles
            - POST /api/smartprofiles/generate
            - POST /api/smartprofiles/search

POWERSHELL COPY COMMANDS:
═════════════════════════

Copy-Item -Path ".\MANUAL_COPY_FILES\contractnest-api\src\groupsService.ts" -Destination ".\contractnest-api\src\" -Force
Copy-Item -Path ".\MANUAL_COPY_FILES\contractnest-api\src\groupsController.ts" -Destination ".\contractnest-api\src\" -Force
Copy-Item -Path ".\MANUAL_COPY_FILES\contractnest-api\src\routes\groupsRoutes.ts" -Destination ".\contractnest-api\src\routes\" -Force

Write-Host "All API files copied!" -ForegroundColor Green

AFTER COPYING:
══════════════
1. Restart your API server (npm run dev in contractnest-api)
2. Test the SmartProfile page at /settings/business-profile/smart-profile
